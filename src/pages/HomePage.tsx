import React from 'react';
import { APP_NAME } from '../utils';

const HomePage: React.FC = () => (
  <div>
    <h1>{APP_NAME} ホーム</h1>
    <p>ようこそ、支援スタッフポータルへ！</p>
  </div>
);

export default HomePage; 